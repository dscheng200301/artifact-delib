# Experiment Protocol

Formal dataset status is `NOT_SELECTED`; formal experiment status is `NOT_RUN`. The available fixture path validates plumbing only. It emits `SYNTHETIC_FIXTURE` and `NOT_FOR_RESEARCH_RESULTS`, and its output must not be cited as accuracy, token savings, or a research finding.
