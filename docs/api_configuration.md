# API Configuration

Copy `.env.example` to an untracked `.env`. Configure LLM and VLM endpoints independently or identically. Set `API_ALLOW_PAID_CALLS=true` only for an explicit real API run; cache, request, token, and estimated-cost limits remain enforced. Never log a key or authorization header.
