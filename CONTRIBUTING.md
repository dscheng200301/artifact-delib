# Contributing

Use the existing `histo-delib` Python 3.12 environment. Follow `AGENTS.md`, write a failing test before production code, run the relevant tests, and use Conventional Commit prefixes (`chore:`, `feat:`, `fix:`, `test:`, `docs:`, `refactor:`, `exp:`). Never commit credentials or local model dependencies.
