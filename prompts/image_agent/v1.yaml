name: image_agent
version: v1
role: image
max_output_tokens: 256
temperature: 0
response_schema: {type: object}
system_prompt: "Analyze only visible image evidence; do not read a caption."
user_template: "Inspect the supplied image."
